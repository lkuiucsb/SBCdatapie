---
title: "`r report_title`"
author: "Generated by `pkg name here`"
date: "`r format(Sys.time(), '%d %B, %Y')`"
output:
  html_document
---

## Data Table Report 


```{r make_report1, echo=F,warning=F,message=F, fig.height=2,fig.width=6}
df <- data_entity_list[["data"]]
head(df)

names(data_entity_list)

print(as.data.frame(dt1))
print(dt2,n=5e2)
#print(dt3,n=5e2)

space_cols <- space_detective(data_entity_list)
names(space_cols)

print(var_report)
```

## Variable Report
```{r make_report2, eval=FALSE, fig.height=2, fig.width=6, message=FALSE, warning=FALSE, include=T}
attach(df)



for(varname in names(df)){
  var<-get(varname)
  if (is.numeric(var)){
     print(varname[1])
     xsummary<-make_numeric_variable_summary_df(var)
     print(kable(xsummary))
     #pander(xsummary)
     x<-make_numeric_histogram(df,var,varname)
     print(x)
  }
  if(is.factor(var) | is.character(var)){
    print(varname[1])
    xsummary<-make_cat_variable_summary_df(var)
    print(kable(xsummary,format="pandoc"))
    #pander(xsummary)
    x<-make_categorical_histogram(df,var,varname)
    print(x)
  }
  if (is.Date(var)) {
    print(varname[1])
    xsummary <- make_time_variable_summary_df(var)
    print(kable(xsummary,format="pandoc"))
    x<-freqclocks_forDates(df,var,varname)
    print(x[1])
    print(x[2])
  }
  if (is.POSIXt(var)) {
    print(varname[1])
    xsummary <- make_time_variable_summary_df(var)
    print(kable(xsummary,format="pandoc"))
    x<-freqclocks_forPOSIX(df,var,varname)
    print(x)
  }
  if(sum(xsummary$num_missing) > 0){
    print("View where missing values occur in the data")
    x<-make_missing_plot(var)
    print(x)
  }
  # spatial heatmap function for variable
  
  space_plot(var)
}

detach(df)
```
